# dsh-archive-manager bundle patch
#
# Mounts the host half (the /archive-manager HTTP routes) into the web profile.
# The browser half is discovered automatically through the `dsh.client`
# declaration in package.json.
- insert:
    - id: archive-manager
      name: 'dsh-archive-manager'